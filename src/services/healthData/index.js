import { NullHealthDataProvider } from './HealthDataProvider'

// Swap this for a real provider once Fix-Your-Calorie integration is built.
export const healthDataProvider = new NullHealthDataProvider()
